{
    "version": 2,
    "framework": null,
    "buildCommand": "mkdir -p build && cp -r assets src index.html build/",
    "outputDirectory": "build",
    "installCommand": "echo 'Instalação pulada'",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}